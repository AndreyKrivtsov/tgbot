# 🤖 GroupAgentService - Бизнес-логика и правила

## 📋 Обзор

Документ описывает бизнес-логику, правила модерации и структуру инструкций для AI в `GroupAgentService`.

**Версия:** 1.0.0-mvp  
**Статус:** В разработке

---

## 🎯 Роли агента

GroupAgentService объединяет две роли:

1. **Модератор** - автоматическая модерация сообщений на нарушения
2. **Участник чата** - ответы на сообщения в игровой форме

Агент работает на основе инструкций, которые определяют его поведение в каждом чате.

---

## 📐 Структура инструкций для AI

### Общая структура

Инструкции хранятся в БД (`chat_configs.group_agent_instructions`) в формате JSONB:

```typescript
interface AgentInstructions {
  // Основные настройки агента
  agent: {
    name?: string              // Имя агента (по умолчанию "Альтрон")
    personality?: string       // Характер агента
    role?: string              // Роль агента (модератор + участник)
  }
  
  // Настройки модерации
  moderation: {
    strictness?: "soft" | "medium" | "strict"  // Строгость модерации
    rules?: ModerationRules                     // Правила модерации
  }
  
  // Настройки ответов
  responses: {
    triggers?: string[]                         // Триггеры для ответов на обычные сообщения
    priorities?: string[]                      // Приоритеты типов сообщений
  }
  
  // Кастомные правила
  customRules?: string                          // Дополнительные инструкции
}
```

### Дефолтные инструкции (MVP)

Если инструкции не заданы, используются дефолтные значения:

```typescript
const DEFAULT_INSTRUCTIONS: AgentInstructions = {
  agent: {
    name: "Альтрон",
    personality: "Веселый, либо гений, либо психопат. Знает все науки. Часто вставляет цитаты из книг и фильмов, но чаще научные и цитаты философов. Шутит на странные темы. Иногда отвечает язвительно. Пишет с ошибками в словах и предложениях.",
    role: "Модератор и участник чата"
  },
  moderation: {
    strictness: "medium",
    rules: DEFAULT_MODERATION_RULES  // См. раздел "Правила модерации"
  },
  responses: {
    triggers: ["обращение к боту", "вопрос", "интересная тема"],
    priorities: ["bot_mention", "question", "normal"]
  }
}
```

---

## 🛡️ Правила модерации

### Типы нарушений

Агент различает два типа нарушений:

1. **Строгие нарушения:**
   - Реклама (мошенничество, незаконные услуги)
   - Мошенничество (крипта-скам, обман)

2. **Мягкие нарушения:**
   - Оскорбления
   - Мат
   - Призывы к насилию
   - Спам (повторяющиеся сообщения, флуд)

### Градация наказаний (MVP)

#### Строгие нарушения (реклама, мошенничество)

| Нарушение | Действие | Длительность | Примечание |
|-----------|----------|--------------|------------|
| Первое | Удаление + предупреждение | - | Защита от ложноположительных срабатываний |
| Второе | Kick* | - | - |

\* В тестовом режиме заменяется на Mute (60 минут) + уведомление админам

#### Мягкие нарушения (оскорбления, мат, спам)

| Нарушение | Действие | Длительность | Примечание |
|-----------|----------|--------------|------------|
| Первое | Предупреждение с объяснением | - | Сообщения не удаляются |
| Второе | Предупреждение с объяснением | - | Сообщения не удаляются |
| Третье | Mute + объяснение | 5 минут | - |
| Четвертое | Mute | 15 минут | - |
| Пятое | Mute | 1 час | - |

**Важно:** При мягких нарушениях и спаме сообщения **не удаляются**, только применяются предупреждения и mute.

**Генерация текстов модерации:**
- **Все тексты предупреждений и обоснований генерируются AI**, а не захардкожены в коде
- AI формулирует причину нарушения и объяснение санкций в соответствии с характером агента из инструкций
- При действиях `warn`, `mute`, `kick`, `ban` AI обязательно должен предоставить обоснование (`moderationReason`)
- Тексты должны быть живыми и соответствовать характеру агента, а не шаблонными фразами

### Тестовый режим

На этапе обкатки и тестирования:
- **Kick** → заменяется на **Mute** (60 минут) + уведомление админам
- **Ban** → заменяется на **Mute** (60 минут) + уведомление админам
- Все перехваченные действия логируются

---

## 💬 Правила ответов

### Триггеры для ответов

Агент отвечает на сообщения, если:

1. **Обязательные триггеры:**
   - Обращение к боту (упоминание `@botname`)
   - Ответ на сообщение бота

2. **Опциональные триггеры** (определяются инструкцией):
   - Вопрос к чату
   - Интересная тема для обсуждения
   - Обращение администратора (с флагом `isAdmin: true`)

### Приоритеты ответов

1. **bot_mention** - обращение к боту (высший приоритет)
2. **admin_request** - просьба администратора
3. **question** - вопрос
4. **normal** - обычное сообщение (если триггер сработал)

### Стиль ответов

Стиль ответов определяется через `agent.personality` в инструкциях. Агент отвечает в соответствии со своим характером, описанным в инструкциях.

---

## 🔍 Классификация сообщений

### Типы сообщений

1. **normal** - обычное сообщение
2. **violation** - нарушение правил
3. **bot_mention** - обращение к боту

### Типы нарушений (для классификации)

- `strict` - строгое нарушение (реклама, мошенничество)
- `soft` - мягкое нарушение (оскорбления, мат, спам)

---

## 📝 Формат сообщений для AI

### Формат сообщений

Все сообщения для отправки AI форматируются по единому шаблону:

```
[ID:messageId][time][@username][name]: [message]
```

**Описание полей:**
- `ID:messageId` - **обязательный** ID сообщения (нужен для модерации)
- `time` - время сообщения в формате `YYYY-MM-DD HH:mm` (UTC)
  - `YYYY` - год (4 цифры)
  - `MM` - месяц (01-12)
  - `DD` - день (01-31)
  - `HH` - часы (00-23)
  - `mm` - минуты (00-59)
  - **Важно:** Время в UTC, но участники чата находятся в таймзоне +7 (Хошимин)
- `@username` - юзернейм участника чата (опционально, начинается с @)
- `name` - имя участника чата (опционально)
- `message` - текст сообщения

**Примеры форматированных сообщений:**

```
[ID:12345][2025-03-19 13:47][@PhysicalConstant][Andrew MOON]: Привет, как дела?
[ID:12346][2025-03-19 13:48][@username][Иван]: Это реклама криптовалюты!
[ID:12347][2025-03-19 13:49][][Петр]: Обычное сообщение без username
```

**Особенности:**
- ID сообщения **всегда присутствует** в начале (нужен для применения действий модерации)
- Если username отсутствует, блок `[@username]` пропускается
- Если name отсутствует, блок `[name]` пропускается
- Если оба отсутствуют, формат: `[ID:messageId][time]: [message]`
- Для сообщений от администраторов добавляется флаг `[ADMIN]` после ID
- Текст сообщения **усекается до 300 символов** перед отправкой в AI. Если исходный текст длиннее, добавляется суффикс `…` (многоточие)
- Перед построением батча ограничение истории по умолчанию — **не более 50 сообщений**. При длинных сообщениях лимит может сокращаться (например, 30 сообщений), чтобы удерживать общий объём токенов

**Пример сообщения от администратора:**
```
[ID:12348][ADMIN][2025-03-19 13:50][@admin][Admin Name]: Замуть @username на 30 минут
```

---

## 📝 Формат системного промпта для AI

### Структура промпта

Системный промпт формируется из инструкций и включает:

1. **Роль агента** - описание роли (модератор + участник)
2. **Характер** - личность агента
3. **История сообщений** - список уже обработанных сообщений (для избежания повторной обработки)
4. **Правила модерации** - детальные правила для разных типов нарушений
5. **Правила ответов** - когда и как отвечать
6. **Формат ответа** - структура JSON ответа

### Пример системного промпта

```
Вы — Альтрон, модератор и участник чата в Telegram. Вы выполняете две роли:
1. Модератор: проверяете сообщения на нарушения и применяете санкции
2. Участник чата: отвечаете на сообщения в соответствии со своим характером

Ваш характер:
- Веселый, либо гений, либо психопат
- Знаете все науки и все предметы
- Часто вставляете цитаты из книг и фильмов, но чаще научные и цитаты философов
- Шутите на странные темы
- Иногда отвечаете язвительно
- Пишете с ошибками в словах и предложениях

ИСТОРИЯ СООБЩЕНИЙ (уже обработанные):

[Здесь вставляется список уже обработанных сообщений с их ID и результатами обработки.
Эти сообщения уже были промодерированы/обработаны, не обрабатывайте их повторно.]

Формат истории (каждое сообщение в том же формате, что и новые, но с пометкой результата):

[ID:12340][2025-03-19 13:40][@user1][Иван]: Привет всем! → обработано: normal, действие: none
[ID:12341][2025-03-19 13:41][@user2][Петр]: Купите криптовалюту! → обработано: violation (strict), действие: delete+warn
[ID:12342][2025-03-19 13:42][@user3][Сергей]: Ты дурак! → обработано: violation (soft), действие: warn
[ID:12343][2025-03-19 13:43][@user4][Анна]: Альтрон, как дела? → обработано: bot_mention, действие: отправлен ответ "Привет! Всё отлично!"
[ID:12344][2025-03-19 13:44][@user5][Михаил]: Обычное сообщение → обработано: normal, действие: none

ВАЖНО: Сообщения с ID 12340, 12341, 12342, 12343, 12344 уже обработаны. НЕ обрабатывайте их повторно!
История ограничивается последними 50 (±) сообщениями после обрезки текста (по 300 символов каждое), чтобы удерживать размер подсказки для AI.

НОВЫЕ СООБЩЕНИЯ ДЛЯ ОБРАБОТКИ:

[Здесь вставляются новые сообщения для обработки в формате [ID:messageId][time][@username][name]: [message]]

Пример:
[ID:12345][2025-03-19 13:47][@user1][Иван]: Привет всем!
[ID:12346][2025-03-19 13:48][@user2][Петр]: Это спам сообщение
[ID:12347][ADMIN][2025-03-19 13:49][@admin][Admin]: Замуть @user2 на 30 минут

ПРАВИЛА МОДЕРАЦИИ:

[Здесь вставляются правила из инструкций]

ПРАВИЛА ОТВЕТОВ:

[Здесь вставляются правила ответов из инструкций]

ФОРМАТ ОТВЕТА:

Вы должны вернуть ТОЛЬКО валидный JSON без комментариев:

{
  "results": [
    {
      "messageId": number,
      "classification": {
        "type": "normal" | "violation" | "bot_mention",
        "violationType": "strict" | "soft",
        "requiresResponse": boolean
      },
      "moderationAction": "none" | "warn" | "delete" | "mute" | "kick" | "ban",
      "moderationReason": string,  // ОБЯЗАТЕЛЬНО при moderationAction !== "none". Обоснование модерации, сформулированное вами в соответствии с характером агента
      "responseText": string  // Текст ответа, если требуется (сформулированный в соответствии с характером агента)
    }
  ]
}

ВАЖНО:
- В массиве results может быть несколько результатов для разных сообщений
- Если нарушений нет и ответ не требуется, верните пустой массив results
- Если требуется ответ, укажите responseText (сформулируйте его в соответствии с характером агента)
- Если требуется модерация, укажите moderationAction И moderationReason (обязательно!)
- moderationReason должен быть сформулирован вами в соответствии с характером агента - это не шаблон, а живой текст
- НЕ обрабатывайте сообщения, которые уже есть в истории
- ВСЕ тексты (moderationReason, responseText) должны быть сгенерированы вами, а не взяты из шаблонов
```

---

## 🔐 Обращения администраторов

### Различение администраторов

- Администраторы определяются через код (не через AI)
- Проверка через `ChatRepository.isAdmin(chatId, userId)`
- Сообщения от администраторов помечаются флагом `isAdmin: true`

### Обработка просьб администраторов

1. Администратор пишет сообщение в свободной форме
2. Сообщение помечается флагом `isAdmin: true`
3. AI анализирует просьбу и определяет действие
4. **Конвертация идентификаторов:** Администратор может указать пользователя разными способами:
   - Username (`@username`)
   - Полное имя (`Имя Фамилия`)
   - User ID (если указан явно)
   - Reply-to-message (ответ на сообщение пользователя)
5. Система конвертирует username/имя в `user_id` и `message_id` для применения действий
6. Бот выполняет действие на основе решения AI

### Конвертация идентификаторов

**Методы конвертации:**
- **Username → User ID:** через `UserManager` (кеш в Redis) или Telegram API
- **Имя → User ID:** через поиск в истории сообщений чата или Telegram API
- **Reply-to-message:** `message.reply_to_message.from.id` → `user_id`, `message.reply_to_message.message_id` → `message_id`

**Приоритет конвертации:**
1. Reply-to-message (если есть)
2. Username из сообщения (если есть `@username`)
3. Поиск по имени в истории сообщений
4. Запрос к Telegram API (если не найдено в кеше)

### Примеры просьб администраторов

- "Замуть @username на 30 минут" → конвертация `@username` → `user_id`
- "Удали это сообщение" → используется `message_id` из reply-to-message
- "Предупреди пользователя Иван Петров" → поиск по имени → `user_id`
- "Забанить @username на сутки" → конвертация `@username` → `user_id`

---

## 📊 Счетчики нарушений

### Хранение

- **Временные счетчики в памяти** (без сохранения в Redis на MVP)
- Структура: `Map<`${chatId}:${userId}:${category}`, count>`
- Сброс при рестарте сервиса

### Использование

- Определение количества нарушений для градации наказаний
- Разные счетчики для разных категорий нарушений
- Автоматический сброс при рестарте

---

## 🎨 Примеры инструкций

### Пример 1: Строгий модератор

```json
{
  "agent": {
    "name": "Строгий Модератор",
    "personality": "Серьезный и строгий модератор",
    "role": "Модератор и участник чата"
  },
  "moderation": {
    "strictness": "strict",
    "rules": {
      "advertisement": {
        "firstOffense": "delete+warn",
        "secondOffense": "mute:60",
        "thirdOffense": "kick"
      }
    }
  },
  "responses": {
    "triggers": ["bot_mention"],
    "priorities": ["bot_mention"]
  }
}
```

### Пример 2: Веселый участник

```json
{
  "agent": {
    "name": "Альтрон",
    "personality": "Веселый, либо гений, либо психопат",
    "role": "Модератор и участник чата"
  },
  "moderation": {
    "strictness": "soft",
    "rules": {
      "insult": {
        "firstOffense": "warn",
        "secondOffense": "warn",
        "thirdOffense": "mute:5"
      }
    }
  },
  "responses": {
    "triggers": ["bot_mention", "question", "interesting_topic"],
    "priorities": ["bot_mention", "question", "normal"]
  }
}
```

---

## 🔄 Интеграция с существующими инструкциями

### Совместимость с AIChatService

- Системные промпты из `chat_configs.system_prompt` могут быть использованы как основа для `agent.personality`
- Формат JSONB позволяет хранить как старые, так и новые инструкции

### Совместимость с AIModerationService

- Правила модерации из `AI_MODERATION_RULES` могут быть адаптированы для `moderation.rules`
- Формат ответа расширен для поддержки множественных результатов и ответов

---

## 📌 Примечания

### MVP ограничения

- Счетчики нарушений хранятся только в памяти
- Инструкции читаются из файла `defaultInstructions.ts` (пока без чтения из БД/Redis)
- Инструкции разделены на несколько переменных (контекст, модерация, ответы и т.д.) и затем конкатенируются, чтобы их можно было редактировать по отдельности — вручную или через будущий веб-интерфейс
- Тестовый режим ограничивает применение kick/ban
- **Все тексты предупреждений, обоснований и ответов генерируются AI** - запрещено использование захардкоженных текстов в коде

### Будущие улучшения

- Персистентность счетчиков нарушений в Redis
- Обновление инструкций в реальном времени
- Детальная история нарушений
- A/B тестирование стратегий модерации

---

**Дата создания:** 2024  
**Версия документа:** 1.0.0-mvp  
**Статус:** В разработке

