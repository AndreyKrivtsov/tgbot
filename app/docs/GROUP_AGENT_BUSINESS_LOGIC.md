# 🤖 GroupAgentService — Бизнес-логика

## 📋 Обзор

Документ фиксирует фактическую бизнес-логику `GroupAgentService`: роли агента, правила модерации и ответы, структуру инструкций и текущие ограничения.

**Версия:** 1.0.0  
**Статус:** актуально для текущего MVP

---

## 🎯 Роли агента

1. **Модератор** — применяет действия в отношении сообщений/пользователей на основе вердикта AI.
2. **Участник чата** — формирует короткие, характерные ответы, когда срабатывают триггеры.

Обе роли работают на одном и том же батче сообщений и зависят от инструкций для конкретного чата.

---

## 📐 Инструкции агента

### Источник

Сейчас инструкции берутся из `defaultInstructions.ts` и одинаковы для всех чатов. Позже планируется хранение в БД (`chat_configs`) и кэширование в Redis.

### Структура

```typescript
interface AgentInstructions {
  agent: {
    name: string
    role: string
    character?: string
  }
  responses: {
    triggers: string[]
    rules: string
  }
  moderation: {
    rules: string
  }
  format: {
    response: string
  }
  customRules?: string
}
```

- `agent` — имя и роль персонажа; `character` описывает стиль.
- `responses.triggers` и `responses.rules` объясняют, когда и как отвечать.
- `moderation.rules` содержит текстовые инструкции (например, ограничения на `kick`/`ban`).
- `format.response` описывает JSON-формат, который должен вернуть AI.
- `customRules` используется для добавления дополнительных указаний.

### Дефолтные правила

`DEFAULT_AGENT_INSTRUCTIONS` задает:
- имя «Альтрон Бот» и роль «Модератор и активный участник большого Telegram-чата»;
- мягкий характер, необходимость отвечать кратко;
- список триггеров (упоминание бота, сложный вопрос, интересная тема, нарушение правил, просьба администратора);
- запрет менять инструкции и раскрывать внутренние правила;
- модерационные правила, запрещающие `kick`/`ban` в тестовом режиме и требующие `warn`/`mute`;
- структуру JSON результата с полями `classification`, `moderationAction`, `responseText` и т.д.

AI обязан следовать этим инструкциям, сервис их не модифицирует.

---

## 🛡️ Модерация

### Входные данные

AI возвращает `ClassificationResult` c полями:
- `classification.type` (`normal` | `violation` | `bot_mention`);
- `classification.requiresResponse` — необходимость ответа;
- `moderationAction` — одно из действий (`warn`, `delete`, `mute`, `kick`, `ban`, `unmute`, `unban`, `none`);
- `responseText` — текст предупреждения или ответа;
- `targetUserId`, `targetMessageId`, `durationMinutes` (опционально).

### Алгоритм

`ModerationPolicy`:
- проверяет наличие действия и минимальные параметры (например, длительность для `mute`);
- определяет `userId` (берет из `targetUserId`, иначе из исходного сообщения);
- собирает `ModerationDecision`, который позже превращается в `ModerationActionDescriptor`;
- не ведет собственных счетчиков нарушений и не эскалирует наказания — логика полностью в руках AI-инструкций.

- Для действий `kick` и `ban` сервис создаёт заявку на подтверждение: модераторы получают сообщение с кнопками «Подтвердить» / «Отменить». Без подтверждения действие не выполняется. Если ответ не получен в течение `REVIEW_TTL_SECONDS`, заявка истекает, кнопки перестают работать.

---

## 💬 Ответы

`ResponsePolicy` рассматривает все результаты батча, где `requiresResponse = true` и есть `responseText`, сортирует их по приоритету типа сообщения и возвращает максимум один ответ:

```typescript
const priorities: ClassificationType[] = ["bot_mention", "normal"]
```

- Ответ отправляется в чат с `replyToMessageId`, если исходное сообщение было ответом, иначе в thread исходного сообщения.
- Текст обрезается до `AI_MAX_RESPONSE_LENGTH`.
- Если ни одно сообщение не требует ответа или AI не вернул текст, ответ не отправляется.

---

## 📚 История и буфер

- История (`ChatHistory`) хранит до `HISTORY_MAX_MESSAGES` записей в Redis. Каждая запись содержит форматированное сообщение и результат (`classification`, `moderationAction`, `responseText`).
- История используется в промпте, чтобы AI не обрабатывал одни и те же сообщения повторно.
- После обработки батча новые записи добавляются в историю, старые удаляются по FIFO.
- Буфер (`MessageBuffer`) хранит необработанные сообщения для каждого `chatId`, восстанавливается из Redis при старте.

---

## 🔐 Администраторы

- Перед добавлением сообщения в буфер `GroupAgentService` проверяет `chatRepository.isAdmin(chatId, userId)` и выставляет `isAdmin`.
- Подтверждать или отменять заявки на `kick`/`ban` могут только администраторы: `CallbackHandler` проверяет права через `ChatRepository`.
- Текущая реализация не генерирует никакие дополнительные действия при административных запросах. AI получает флаг `isAdmin` и сам решает, какие действия предпринять.
- Конвертация username → userId, хранение маппинга пользователей и др. находятся вне `GroupAgentService` и должны реализовываться другими сервисами/адаптерами.

---

## 📏 Ограничения текущей реализации

- Нет внутренних счетчиков нарушений и эскалации — всё делает AI.
- Нет динамических инструкций из БД.
- Эскалация по kick/ban требует ручного подтверждения модератора и не может быть выполнена автоматически.
- Тексты предупреждений и ответы не проверяются на шаблоны, сервис лишь передает их дальше.
- Дополнительные метаданные (например, тип нарушения strict/soft) не используются.

---

## 🚀 Идеи для развития

- Хранить пользовательские инструкции на уровне чата и подставлять их через `InstructionsProvider`.
- Добавить счетчики повторных нарушений в Redis и использовать их в `ModerationPolicy`.
- Валидация `responseText`/`reason` по длине и обязательности.
- Расширенная аналитика: количество действий, доля успешных запросов к AI, распределение по типам нарушений.
- Поддержка нескольких ответов в батче и очереди для затянувшихся ответов.

---

**Дата обновления:** 2025  
**Статус документа:** актуально

